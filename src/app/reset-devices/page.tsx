import React, { Suspense } from "react";
import ResetDeviceSessionsContent from "./content";

const ResetDeviceSessionsPage = () => {
  return (
    <Suspense>
      <ResetDeviceSessionsContent />
    </Suspense>
  );
};

export default ResetDeviceSessionsPage;
